"""Recommendation engine modules.

Submodule imports are available via their direct paths, e.g.::

    from src.recommendations.engine import RecommendationEngine
    from src.recommendations.scorers import GenreMatchScorer

Direct submodule imports are required; package-level re-exports have been
removed to prevent circular import chains.
"""
